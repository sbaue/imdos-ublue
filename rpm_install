## This file contains the desired ostree layering, @spec files are allowed. This runs after rpm_overlay. I advise testing the installation of the packages with rpm-ostree install --dry-run before adding them here.

vim kate
